[user]
# CUSTOMIZE(name)
  name = Sam King
  email = sam@samking.org
[push]
	default = simple
[color]
  # colorize by default
	ui = true
[core]
  # necessary to prevent git log from truncating, rather than wrapping, lines.
	pager = less -r

# vim: ft=gitconfig
