--sort-files
--smart-case
# har files are http archives.  These might all be on one line, which annoyingly
# creates a lot of matches that aren't useful.
--ignore-file=ext:har
--ignore-file=ext:svg
# Python Coverage reports aren't useful.
--ignore-dir=htmlcov
--ignore-file=match:.coverage
